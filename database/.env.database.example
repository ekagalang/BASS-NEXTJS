# ============================================
# BASS Training Academy - Database Configuration
# ============================================
# Copy file ini menjadi .env.database dan sesuaikan dengan konfigurasi Anda
# File .env.database tidak akan di-commit ke git (sudah ada di .gitignore)
# ============================================

# Prisma Database URL
# Format for MySQL: mysql://USER:PASSWORD@HOST:PORT/DATABASE
# Format for PostgreSQL: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="mysql://root:your_password@localhost:3306/bass_training"

# MySQL Connection Settings (for setup scripts)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=bass_training

# MySQL Root User (untuk setup script)
# PENTING: Jangan commit password ke git!
DB_ROOT_USER=root
# DB_ROOT_PASSWORD=your_password_here  # Uncomment dan isi jika diperlukan

# Application Database User (recommended untuk production)
# Buat user terpisah dengan limited privileges untuk aplikasi
DB_USER=bass_app_user
# DB_PASSWORD=your_app_password_here  # Uncomment dan isi jika diperlukan

# ============================================
# NOTES
# ============================================
#
# Untuk Development:
# - Gunakan DB_ROOT_USER untuk setup database
# - Password akan di-prompt saat menjalankan setup script
#
# Untuk Production:
# - Buat user database terpisah dengan command:
#   CREATE USER 'bass_app_user'@'localhost' IDENTIFIED BY 'password';
#   GRANT SELECT, INSERT, UPDATE, DELETE ON bass_training.* TO 'bass_app_user'@'localhost';
#   FLUSH PRIVILEGES;
#
# - Gunakan DB_USER dan DB_PASSWORD untuk koneksi aplikasi
# - Jangan gunakan root user untuk aplikasi production
#
# Security Best Practices:
# - Jangan commit file .env.database ke git
# - Gunakan strong password
# - Limit user privileges sesuai kebutuhan
# - Backup database secara berkala
#
